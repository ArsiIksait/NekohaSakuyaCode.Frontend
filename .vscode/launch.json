{
  // 使用 IntelliSense 了解相关属性。
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge", // 如使用 Edge，改为 "msedge", 如使用chrome, 改为 "chrome"
      "request": "launch",
      "name": "启动 ClientApp",
      "url": "http://localhost:5173", // 与 Vite 实际地址一致
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "确保 Vite 已启动" // 必须与 tasks.json 中的 label 完全一致
    }
  ]
}
